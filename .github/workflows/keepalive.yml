name: Keep API Awake
on:
  schedule:
    - cron: '*/10 * * * *'
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API health
        continue-on-error: true
        run: curl -sf --max-time 30 ${{ secrets.RENDER_API_URL }}/api/health > /dev/null
